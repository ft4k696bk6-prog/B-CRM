name: Refresh stable demo alias

on:
  push:
    branches:
      - codex/bcrm-demo-portfolio

jobs:
  alias:
    if: github.event.head_commit.message == 'Point stable demo alias to refreshed CRM'
    runs-on: ubuntu-latest
    steps:
      - name: Point stable demo alias to refreshed CRM
        run: npx vercel alias set "https://b-8zqzncfoj-berni.vercel.app" "b-crm-demo-berni.vercel.app" --token "$VERCEL_TOKEN"
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
