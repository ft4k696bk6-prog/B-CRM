name: Emergency Lead Cleanup

on:
  workflow_dispatch:

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - name: Reset stale meetings and assignments
        run: |
          curl --fail-with-body --show-error --silent \
            --request POST \
            --header "x-cleanup-key: cleanup-20260722-x9K4mQ7vP2sL8nR5" \
            "https://b-7h61wgro3-berni.vercel.app/api/admin/leads/emergency-cleanup"
